# Server Configuration
server.port=8888

# Database Configuration (Local MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/reals?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=sajidsai
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:5175,https://real-estate-alpha-sandy.vercel.app
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# DevTools (only effective in development)
# Enable automatic restart (default true)
spring.devtools.restart.enabled=true
# Enable LiveReload (default true)
spring.devtools.livereload.enabled=true
# Additional paths to watch for changes (optional)
spring.devtools.restart.additional-paths=src/main/resources,src/main/java
# Exclude static template/resource folders from triggering restarts (optional)
spring.devtools.restart.exclude=static/**,public/**,templates/**
# Activate 'dev' profile by default (optional; adjust as needed)
spring.profiles.active=dev

# Payments (Razorpay) - read from environment variables
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_RCPVxYzrDXLFvl}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:6cNCya6jdh1fjNQ5k8IwOyYE}

# JWT Configuration
# Ensure the secret is at least 32 characters (256 bits) for HS256
jwt.secret=${JWT_SECRET:ThisIsADevelopmentOnlyJWTSecretKeyThatIsAtLeast32Chars}
jwt.expiration=${JWT_EXPIRATION_MS:86400000}

# SMTP Mail (Gmail)
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:ss2727303@gmail.com}
spring.mail.password=${SMTP_PASSWORD:vzjr xyav iloc tacj}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
